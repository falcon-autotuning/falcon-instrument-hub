name: UPLOAD_DATA
version: 1.0.0
description: Used by the interpreter to hand data off the the runtime for FAlCon

parameters:
  <<: *timestamp

  data:
    type: jsonable
    required: true
    description: the jsonable measurement request for the FAlCon to unpack and use
